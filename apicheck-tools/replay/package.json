{
  "name": "replay",
  "version": "1.0.0",
  "description": "Will send the traffic to the same place, in the same order, with the same params.",
  "main": "./lib/index.js",
  "bin": {
    "ac-replay": "./lib/index.js"
  },
  "scripts": {
    "test": "mocha -r ts-node/register src/**/*.spec.ts",
    "build": "tsc -p .",
    "clear": "rm -rf ./node_modules ./package-lock.json && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": ">=0.21.1",
    "commander": "^4.0.0",
    "jsonschema": "^1.2.4",
    "rxjs": "^6.5.3"
  },
  "devDependencies": {
    "@types/mocha": "^5.2.7",
    "@types/node": "^12.12.6",
    "chai": "^4.2.0",
    "mocha": "^6.2.2",
    "ts-node": "^8.4.1",
    "typescript": "^3.7.2"
  }
}
